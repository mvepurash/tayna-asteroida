// ============================================================
// config.js — все константы игры в одном месте
// Менять баланс только здесь, не в логике модулей
// ============================================================

const CONFIG = {

  // --- Канвас ---
  CANVAS_W: 480,        // внутреннее разрешение (px), масштабируется CSS
  CANVAS_H: 854,

  // --- Кислород ---
  OXYGEN_MAX: 90,       // секунд (п. 1.3 ТЗ)

  // --- Экипаж ---
  CREW_SIZE: 3,         // астронавтов за сессию (п. 1.7 ТЗ)

  // --- Добыча ---
  CRYSTALS_PER_TAP: 1,          // кристаллов за одно нажатие кнопки ДОБЫЧА
  MINING_TAP_INTERVAL: 200,     // мс — минимальный интервал между РУЧНЫМИ тапами
  MINING_HOLD_INTERVAL: 600,    // мс — интервал при УДЕРЖАНИИ кнопки (медленнее)

  // --- Астронавт ---
  ASTRONAUT_MOVE_DURATION: 700, // мс на один переход между платформами
  ASTRONAUT_TURN_POINT: 0.6,    // точка невозврата — 60% пути (В.2 Приложение В)

  // --- Щупальца ---
  // Базовые значения для режима А (только щупальца, сложность Лёгкий)
  TENTACLE_EXTEND_DURATION: 1200, // мс — выдвижение одного сегмента
  TENTACLE_RETRACT_DURATION: 800, // мс — втягивание одного сегмента
  TENTACLE_HOLD_DURATION: 600,    // мс — пауза на максимальной длине
  TENTACLE_WARNING_TIME: 800,     // мс — предупреждение перед атакой (п. 5.14 ТЗ)
  TENTACLE_SLOT_PAUSE: 400,       // мс — пауза между слотами атаки групп А/Б

  // --- Скорость игры (рост сложности) ---
  // GAME_SPEED_MULTIPLIER = 1 + sessionTime / SPEED_RAMP_TIME
  // Потолка нет (В.6 Приложение В)
  SPEED_RAMP_TIME: 120,  // секунд до +1x скорости (калибровать на прототипе)

  // --- Реклама (Яндекс) ---
  REWARDED_AD_COOLDOWN: 30000,   // мс — минимум между RV (п. 10.3 ТЗ)

  // --- Сохранение ---
  SAVE_KEY: 'tayna_asteroida_save',

  // --- Отладка ---
  DEBUG: false,  // отладочная панель отключена
};
